export interface GreenComponentArgs{
    message: string;
}

export interface BlueComponentArgs{
    num: number;
}

export interface RedComponentArgs{
    message: string;
    num: number;
}